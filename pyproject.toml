[project]
name = "softtorch"
version = "0.1.0"
description = "A library for soft differentiable relaxations of common PyTorch functions."
readme = "README.md"
authors = [
    { name = "Anselm Paulus", email = "paulus.anselm@gmail.com" }
]
requires-python = ">=3.12"
license = "Apache-2.0"
keywords = ["differentiable", "soft", "relaxation", "sorting", "ranking", "pytorch"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Typing :: Typed",
]
dependencies = [
    "torch>=2.2.0",
    "pot>=0.9.3",
    "torchopt>=0.7.0",
    "numba>=0.61",
    "numpy>=1.26.0",
]

[project.urls]
Homepage = "https://github.com/a-paulus/softtorch"
Documentation = "https://a-paulus.github.io/softtorch/"
Repository = "https://github.com/a-paulus/softtorch"
Issues = "https://github.com/a-paulus/softtorch/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
exclude = ["uv.lock", "benchmarks/", "docs/", "tests/"]

[dependency-groups]
dev = [
    "hippogriffe>=0.2.2",
    "ipykernel>=6.29.5",
    "ipython>=9.3.0",
    "matplotlib>=3.10.3",
    "mkdocs>=1.6.1",
    "mkdocs-include-exclude-files>=0.1.0",
    "mkdocs-ipynb>=0.1.1",
    "mkdocs-material>=9.6.14",
    "mkdocstrings>=0.29.1",
    "mkdocstrings-python>=1.16.12",
    "mkdocs-jupyter",
    "numpy>=1.26.0",
    "pandas",
    "pymdown-extensions>=10.15",
    "pytest>=8.0.0",
    "ruff>=0.11.12",
]

[tool.ruff]
extend-include = ["*.ipynb"]
src = ["src"]

[tool.ruff.lint]
fixable = ["I001", "F401", "UP"]
ignore = ["E402", "E721", "E731", "E741", "F722", "UP038", "E501"]
select = ["E", "F", "I001", "UP"]

[tool.ruff.lint.isort]
combine-as-imports = true
extra-standard-library = ["typing_extensions"]
lines-after-imports = 2
order-by-type = false
